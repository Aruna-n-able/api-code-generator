anthropic>=0.40.0
pyyaml>=6.0
requests>=2.31.0

# Optional but recommended for local testing
pytest>=8.0.0
pytest-cov>=5.0.0

# Optional fix-template dependencies (not required to run the skill itself)
# freezegun>=1.5.0   # For datetime mocking (recommended fix for timing issues)
# responses>=0.25.0  # For mocking HTTP calls (recommended fix for external API issues)
# faker>=26.0.0      # For deterministic random data generation
